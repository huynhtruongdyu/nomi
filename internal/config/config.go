package config

// Package config manages application settings, user preferences, and environment configuration.
// Use this package to load/save config files, manage environment-specific overrides,
// and provide typed configuration to other packages.
